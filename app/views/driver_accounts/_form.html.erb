<%= form_for([@driver_account.user, @driver_account]) do |f| %>
  <%= render 'shared/errors', resource: @driver_account %>
  <div class="actions">
    <%= f.submit "Crear", { class: "btn btn-outline-primary btn-lg btn-block" } %>
  </div>
<% end %>
