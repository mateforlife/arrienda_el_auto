<%= f.fields_for :address do |ff| %>
  <div class="form-group">
    <%= ff.label :city %>
    <%= ff.grouped_collection_select :city_id,
        State.all, :cities, :name, :id, :name,
        { include_blank: true }, { class:"form-control" } %>
  </div>

  <div class="form-row">
    <div class="form-group col-md-6">
      <%= ff.label 'Calle' %>
      <%= ff.text_field :street, class:"form-control" %>
    </div>
    <div class="form-group col-md-3">
      <%= ff.label 'Número' %>
      <%= ff.text_field :street_number, class:"form-control" %>
    </div>
    <div class="form-group col-md-3">
      <%= ff.label 'Depto/Block' %>
      <%= ff.text_field :apartment, class:"form-control" %>
    </div>
  </div>
<% end %>

