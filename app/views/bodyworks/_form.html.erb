<%= form_with(model: bodywork, local: true) do |form| %>
  <% if bodywork.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bodywork.errors.count, "error") %> prohibited this bodywork from being saved:</h2>

      <ul>
        <% bodywork.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
